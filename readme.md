# Endpoints Added
### /reverse [POST]
reverses the text given

### /square/(int:number) [GET]
returns the squared version of the number


# Issues
- Blank Lines Issue with Lint
- Python Versioning Issue with test.yml (version 3.10 was being seen as 3.1 in github)
- Codeql version was depreciated (updated to version 4 and gave permissions for Actions)